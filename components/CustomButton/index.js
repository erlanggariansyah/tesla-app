import React from "react";
import { View, Text} from "react-native";

const Button = function () {
    return (
        <View>
            <Text>Anggs</Text>
        </View>
    );
};

export default Button;